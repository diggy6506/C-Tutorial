#include <stdio.h>
#include <stdlib.h>
/* %s are words. %d are numbers. %f are decimals. %c are single characters. */
int main()
{



     int favNum = 90;
     char myChar = 'i'
     printf("My favourite %s is %d", myChar, favNum);

    return 0;
}
